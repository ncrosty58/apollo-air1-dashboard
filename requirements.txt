# Pinned to the current major.minor so a `docker compose up --build` can't
# silently pull a breaking release (paho-mqtt 2.x in particular changed the
# callback API this app relies on). `~=X.Y` allows patch/minor updates up to
# the next major. Bump deliberately when upgrading.
flask~=3.1
python-dotenv~=1.2
influxdb-client~=1.50
paho-mqtt~=2.1
requests~=2.32
